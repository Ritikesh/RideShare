<% unless current_user == @user %>
	<div id="follow_form" class="row margin-top margin-bottom">
		<% if current_user.following?(@user) %>
			<%= render 'unfollow' %>
		<% else %>
			<%= render 'follow' %>
		<% end %>
	</div>
<% end %>